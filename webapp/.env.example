# Base URL of the Render API. Build-time only.
VITE_API_BASE=https://your-app.onrender.com

# Optional: override the Vite base path if hosting under a different
# GitHub Pages repo or custom domain.
# VITE_BASE_PATH=/arabic_tutor_bot/
